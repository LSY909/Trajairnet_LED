# TrajAirNet 代码逻辑分析

## 📋 整体架构概览

当前代码实现了一个**基于 LED (Latent Energy Diffusion) + 双重先验信息**的航空轨迹预测模型：

- **核心框架**: LED (扩散模型) + Transformer 去噪器
- **双重先验**: 
  1. **航线聚类先验** (RAG + GMM) - 从历史数据检索并聚类得到的典型航线
  2. **Dense Future Predictor** (MTR 模块) - 可学习的稠密未来预测先验

---

## 🔄 训练流程 (train.py)

### 1. 数据加载阶段

```python
# 支持两种模式：
# A. 离线预计算模式（快）：直接加载预计算的 route_priors.pt
dataset_train = TrajectoryDataset(..., route_priors_path=route_priors_train)

# B. 在线模式（慢）：需要初始化 RAG 系统，训练时实时检索+聚类
if route_priors_train is None:
    rag = TrajectoryDataset_RAG(...).rag_system
    embedder = TimeSeriesEmbedder()
```

**数据格式**:
- `obs_traj`: (B, A, 3, 11) - 观测轨迹 [x, y, z]
- `pred_traj`: (B, A, 3, 12) - 真实未来轨迹
- `route_priors`: (B, A, 3, 12, 3) - 预计算的航线先验 [n_clusters, T, 3]

### 2. 训练循环

每个 batch 的处理流程：

```
输入: obs_traj, pred_traj, context, route_priors (可选)
  ↓
TrajAirNet.forward()
  ↓
【先验信息生成/使用】
  ├─ route_priors: 如果未提供，调用 _get_route_priors() 在线检索+聚类
  └─ dense_future: DenseFuturePredictor 预测 (B, A, T, 7)
  ↓
【LED Initializer】
  ├─ 编码历史轨迹: ego_encoder + social_encoder
  ├─ 编码 route_priors → 32维向量
  ├─ 融合: [ego(256) + social(256) + prior(32)] → mean/var/scale
  └─ 输出: guess_var (B*A, 20, 12, 3), guess_mean (B*A, 12, 3), guess_scale
  ↓
【融合 Dense Future Prior】
  └─ mean_estimation[:, :, 0:2] = (1-w)*mean + w*dense_xy
  ↓
【扩散采样】
  ├─ loc = sample_prediction + mean_estimation
  └─ generated_y = p_sample_loop_accelerate() - 5步去噪
  ↓
【损失计算】
  ├─ loss_dist: 最小模态的 L2 距离损失
  └─ loss_uncertainty: 不确定性正则化损失
  ↓
反向传播 + 优化器更新
```

---

## 🧠 先验信息详解

### 先验 1: 航线聚类先验 (Route Priors via RAG+GMM)

#### 生成方式（三种模式）

**A. 离线预计算** (最快，推荐)
```python
# 运行 precompute_route_priors.py 一次性生成
# 输出: route_priors_7days1_train.pt
# 格式: Dict[scene_idx: (A, n_clusters, T, 3)]
```

**B. 在线 per_batch** (默认，较快)
```python
# 每个 batch 只拟合一次 GMM，所有 agent 共享聚类中心
gmm = GaussianMixture(n_components=3).fit(rel.reshape(-1, T*3))
ctrs = gmm.means_.reshape(3, T, 3)  # (C, T, 3)
# 广播到所有 agent: (B, A, C, T, 3)
```

**C. 在线 per_agent** (最慢，最精细)
```python
# 每个 agent 单独拟合 GMM
for each agent:
    gmm.fit(agent_k_retrieved_trajs)
    ctrs[agent] = gmm.means_  # (C, T, 3)
```

#### 使用方式

在 `LEDInitializer.forward()` 中：

```python
# 1. 编码 route_priors: (B*A, C, T, 3) → (B*A, 32)
route_rel = route_priors - last_obs_pos  # 转为相对坐标
points = route_rel.reshape(B*A, C*T, 3)
pt_feat = route_point_mlp(points)        # (B*A, C*T, 64)
pooled = pt_feat.mean(dim=1)              # (B*A, 64)
priors_embed = route_agg(pooled)          # (B*A, 32)

# 2. 融合到三个分支
mean_total = [ego(256) + social(256) + prior(32)]  → mean_decoder
scale_total = [ego(256) + social(256) + prior(32)] → scale_decoder  
var_total = [ego(256) + social(256) + scale_feat(32) + prior(32)] → var_decoder
```

**影响**: route_priors 会同时影响 `guess_mean`、`guess_scale`、`guess_var`，从而影响扩散的初始分布。

---

### 先验 2: Dense Future Predictor (MTR 模块)

#### 工作原理

```python
# 输入
obj_feature = ego_mean_encoder(past_traj)  # (B, A, 256) - 历史特征
obj_pos = x[:, :, :, -1]                    # (B, A, 3) - 当前位置

# DenseFuturePredictor.forward()
obj_pos_feature = obj_pos_encoding_layer(obj_pos[..., 0:2])  # (B, A, 256)
fused = [obj_pos_feature(256) + obj_feature(256)]            # (B, A, 512)
pred_dense_trajs = dense_future_head(fused)                  # (B, A, T, 7)

# 输出格式: (B, A, T, 7)
# [:, :, :, 0:2] = (x, y) 位置
# [:, :, :, 2:5] = GMM 参数 (mu_x, mu_y, sigma_x, sigma_y, rho)
# [:, :, :, 5:7] = (vx, vy) 速度
```

#### 融合方式

```python
# 只取位置 (x, y) 作为先验
dense_xy = pred_dense_trajs[..., 0:2]  # (B, A, T, 2)

# 加权融合到 mean_estimation
w = dense_prior_weight  # 默认 0.5
mean_estimation[:, :, 0:2] = (1.0 - w) * mean_estimation[:, :, 0:2] + w * dense_xy
```

**影响**: 只修正 `mean_estimation` 的前两维 (x, y)，第三维 (z) 保持 LED 原始输出。

---

## 🎯 LED Initializer 详细流程

### 输入
- `x`: (B*A, 11, 3) - 历史轨迹（已 flatten batch 和 agent）
- `mask`: (B*A, B*A) - 社交交互 mask
- `route_priors`: (B, A, C, T, 3) 或 None

### 编码阶段

```python
# 1. 社交编码
social_embed = social_transformer(x, mask)  # (B*A, 1, 256) → squeeze → (B*A, 256)

# 2. 自身编码（三个分支）
ego_var_embed = ego_var_encoder(x)    # (B*A, 256)
ego_mean_embed = ego_mean_encoder(x)  # (B*A, 256)
ego_scale_embed = ego_scale_encoder(x) # (B*A, 256)

# 3. 航线先验编码（如果提供）
if route_priors is not None:
    priors_embed = encode_route_priors(route_priors)  # (B*A, 32)
else:
    priors_embed = zeros(B*A, 32)
```

### 解码阶段

```python
# Mean 分支
mean_total = [ego_mean(256) + social(256) + prior(32)]  # (B*A, 544)
guess_mean = mean_decoder(mean_total)  # (B*A, 12*3) → reshape → (B*A, 12, 3)

# Scale 分支
scale_total = [ego_scale(256) + social(256) + prior(32)]  # (B*A, 544)
guess_scale = scale_decoder(scale_total)  # (B*A, 1)

# Variance 分支
scale_feat = scale_encoder(guess_scale)  # (B*A, 32)
var_total = [ego_var(256) + social(256) + scale_feat(32) + prior(32)]  # (B*A, 576)
guess_var = var_decoder(var_total)  # (B*A, 20*12*3) → reshape → (B*A, 20, 12, 3)
```

### 输出
- `guess_var`: (B*A, 20, 12, 3) - 20 个模态的方差
- `guess_mean`: (B*A, 12, 3) - 单模态均值
- `guess_scale`: (B*A, 1) - 尺度参数

---

## 🌊 扩散采样流程

### 初始化

```python
# 1. 归一化 sample_prediction
sample_prediction = exp(variance/2) * sample_prediction / std(sample_prediction)

# 2. 融合 mean
loc = sample_prediction + mean_estimation[:, None]  # (B*A, 20, 12, 3)

# 3. 融合 dense future prior (如果启用)
if dense_prior_weight > 0:
    mean_estimation[:, :, 0:2] = (1-w)*mean + w*dense_xy
```

### 去噪过程 (p_sample_loop_accelerate)

```python
# 分两段去噪（加速策略）
cur_y = loc[:, :10]   # 前 10 个模态
for i in reversed(range(5)):  # NUM_Tau = 5
    cur_y = p_sample_accelerate(x, mask, cur_y, i)

cur_y_ = loc[:, 10:]  # 后 10 个模态
for i in reversed(range(5)):
    cur_y_ = p_sample_accelerate(x, mask, cur_y_, i)

# 拼接
generated_y = [cur_y_, cur_y]  # (B*A, 20, 12, 3)
```

### 单步去噪 (p_sample_accelerate)

```python
# 1. 预测噪声
eps_theta = TransformerDenoisingModel.generate_accelerate(cur_y, beta, x, mask)

# 2. 计算去噪后的均值
mean = (1/sqrt(alpha)) * (cur_y - eps_factor * eps_theta)

# 3. 添加少量噪声（sigma_t 很小，接近确定性）
sample = mean + sigma_t * z * 0.00001
```

---

## 📊 损失函数

### 总损失

```python
loss = loss_dist * 100 + loss_uncertainty
```

### Loss Dist (距离损失)

```python
# 1. 计算所有模态与真实轨迹的距离
diff = generated_y - fut_traj.unsqueeze(1)  # (B*A, 20, 12, 3)
dist = norm(diff, p=2, dim=-1).mean(dim=-1)  # (B*A, 20) - 每个模态的平均距离

# 2. 选择最佳模态（距离最小）
min_dist = dist.min(dim=1)[0]  # (B*A,)

# 3. 平均
loss_dist = min_dist.mean()
```

### Loss Uncertainty (不确定性损失)

```python
# 1. 计算最佳模态的距离
best_mode_idx = dist.argmin(dim=1)
best_traj = generated_y[batch_idx, best_mode_idx]  # (B*A, 12, 3)
best_dist = (best_traj - fut_traj).norm(p=2, dim=-1).mean(dim=-1)  # (B*A,)

# 2. 不确定性正则化
loss_uncertainty = exp(-variance) * best_dist + variance
loss_uncertainty = loss_uncertainty.mean()
```

**物理意义**: 
- `exp(-variance) * dist`: 当 variance 小时，惩罚大距离；当 variance 大时，允许大距离
- `variance`: 防止 variance 无限增大

---

## 🔍 关键设计点

### 1. 双重先验的互补性

- **Route Priors (RAG+GMM)**: 
  - 来源：历史数据检索 + 无监督聚类
  - 特点：**静态先验**，反映"典型航线模式"
  - 影响：mean/var/scale 三个分支

- **Dense Future Predictor**:
  - 来源：可学习的神经网络
  - 特点：**动态先验**，根据当前历史轨迹自适应预测
  - 影响：只修正 mean 的 (x, y)

### 2. 先验融合策略

```python
# Route priors: 在 LEDInitializer 内部编码融合
mean = decoder([ego + social + route_prior_embed])

# Dense future: 在 LEDInitializer 外部加权融合
mean[:, :, 0:2] = (1-w) * mean[:, :, 0:2] + w * dense_xy
```

**设计思路**: route_priors 提供"全局航线模式"，dense_future 提供"局部精细修正"。

### 3. 加速策略

- **离线预计算**: 避免训练时重复检索+聚类
- **per_batch 聚类**: 减少 GMM 拟合次数
- **5 步去噪**: NUM_Tau=5，而非完整 100 步
- **分模态去噪**: 20 个模态分两批并行去噪

---

## 📈 数据流维度变化

```
输入:
  obs_traj: (B, A, 3, 11)
  route_priors: (B, A, 3, 12, 3) 或 None

Flatten:
  past_traj: (B*A, 11, 3)

LED Initializer:
  ego_embed: (B*A, 256)
  social_embed: (B*A, 256)
  prior_embed: (B*A, 32)
  ↓
  guess_mean: (B*A, 12, 3)
  guess_var: (B*A, 20, 12, 3)
  guess_scale: (B*A, 1)

Dense Future:
  obj_feature: (B, A, 256)
  ↓
  pred_dense: (B, A, 12, 7)
  dense_xy: (B, A, 12, 2) → (B*A, 12, 2)

融合:
  mean_estimation: (B*A, 12, 3)
  mean[:, :, 0:2] = (1-w)*mean + w*dense_xy

扩散采样:
  loc: (B*A, 20, 12, 3)
  ↓
  generated_y: (B*A, 20, 12, 3)

损失计算:
  fut_traj: (B*A, 12, 3)
  loss_dist: scalar
  loss_uncertainty: scalar
```

---

## 🎛️ 可调参数

| 参数 | 默认值 | 作用 |
|------|--------|------|
| `dense_prior_weight` | 0.5 | Dense future 先验的融合权重（0=关闭） |
| `route_prior_mode` | `per_batch` | 航线先验生成模式（per_agent/per_batch/topk/none） |
| `k_retrieve` | 100 | RAG 检索的邻居数量 |
| `n_clusters` | 3 | GMM 聚类中心数量 |
| `NUM_Tau` | 5 | 扩散去噪步数（加速） |

---

## 💡 总结

当前代码实现了**"检索增强 + 双重先验 + LED 扩散"**的轨迹预测框架：

1. **检索增强**: 从历史库中检索相似轨迹，通过 GMM 聚类得到典型航线模式
2. **双重先验融合**: 
   - Route priors 提供全局航线约束（影响 mean/var/scale）
   - Dense future 提供局部位置修正（只影响 mean 的 x,y）
3. **LED 扩散**: 基于先验的初始分布，通过 5 步快速去噪生成多模态预测

**优势**: 
- 先验信息丰富，预测更准确
- 支持离线预计算，训练速度快
- 多模态输出，覆盖不确定性

**潜在改进点**:
- Dense future predictor 目前没有单独的损失项（只在融合时使用）
- Route priors 的编码方式可以尝试更复杂的聚合（如 attention）

